import "./mmrRollUp64" as mmrRollUp64

def main(field root, field width, field[64] xItems, field[64] yItems, field newRoot, private field[16] xPeaks, private field[16] yPeaks) -> (field):
    newRoot == mmrRollUp64(root, width, xPeaks, yPeaks, xItems, yItems)
    return 1
