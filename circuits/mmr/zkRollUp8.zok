import "./mmrRollUp8" as mmrRollUp8

def main(field root, field width, field[8] xItems, field[8] yItems, field newRoot, private field[16] xPeaks, private field[16] yPeaks) -> (field):
    newRoot == mmrRollUp8(root, width, xPeaks, yPeaks, xItems, yItems)
    return 1
