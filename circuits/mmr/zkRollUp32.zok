import "./mmrRollUp32" as mmrRollUp32

def main(field root, field width, field[32] xItems, field[32] yItems, field newRoot, private field[16] xPeaks, private field[16] yPeaks) -> (field):
    newRoot == mmrRollUp32(root, width, xPeaks, yPeaks, xItems, yItems)
    return 1
