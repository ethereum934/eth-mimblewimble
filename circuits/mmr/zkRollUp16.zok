import "./mmrRollUp16" as mmrRollUp16

def main(field root, field width, field[16] xItems, field[16] yItems, field newRoot, private field[16] xPeaks, private field[16] yPeaks) -> (field):
    newRoot == mmrRollUp16(root, width, xPeaks, yPeaks, xItems, yItems)
    return 1
